{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "types": ["node"], // Only include 'node' for production
        "rootDir": ".",
        "outDir": "dist",
    },
    "exclude": ["**/*.test.ts", "knexfile.ts", "node_modules", "dist"]
}